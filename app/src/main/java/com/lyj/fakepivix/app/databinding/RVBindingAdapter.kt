package com.lyj.fakepivix.app.databinding

import android.databinding.BindingAdapter
import android.support.v7.widget.LinearLayoutManager
import android.support.v7.widget.RecyclerView

/**
 * @author greensun
 *
 * @date 2019/3/22
 *
 * @desc
 */

/*
@BindingAdapter(value = ["adapter", "layoutManager"], requireAll = false)
fun RecyclerView.adapter(adapter: RecyclerView.Adapter<RecyclerView.ViewHolder>?, layoutManager: RecyclerView.LayoutManager?) {
    this.adapter = adapter
    when (layoutManager) {
        null -> this.layoutManager = LinearLayoutManager(context)
        else -> this.layoutManager = layoutManager
    }
}*/
